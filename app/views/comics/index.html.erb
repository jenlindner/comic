<ul>
<% @comics.each do |c| %>
	<li><%= link_to c.title, comic_path(c) %></li>
	
<% end %>
</ul>

<% form_for Comic.new, :url => comics_path do |f| %>
	
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.submit "Submit!" %>	
	
<% end %>